import React from 'react';
import MainPage from '../../components/pages/MainPage';
import { cardsData } from '../../helpers';

const MainContainer = () => <MainPage cardsData={cardsData} />;

export default MainContainer;
